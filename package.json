{
  "name": "cline-telemetry-minimal",
  "version": "1.0.0",
  "description": "Minimal telemetry server for Cline (FastAPI backend)",
  "main": "main.py",
  "scripts": {
    "start": "./start.sh",
    "dev": "source venv/bin/activate && python -m uvicorn main:app --host 0.0.0.0 --port 8011 --reload"
  },
  "engines": {
    "python": ">=3.8"
  },
  "keywords": ["telemetry", "fastapi", "cline", "analytics"],
  "author": "",
  "license": "MIT"
}
